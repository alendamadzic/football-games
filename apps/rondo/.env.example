# Convex deployment URL for this app (bunx convex dev writes this to .env.local)
NEXT_PUBLIC_CONVEX_URL=

# Transfermarkt API base URL. Defaults to the shared production deployment;
# point at http://localhost:8000 to run against apps/transfermarkt-api locally.
TM_API_URL=
